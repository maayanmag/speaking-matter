# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Python (for tools/)
__pycache__/
*.pyc
.venv/
venv/

# Node (if we ever add a dev server)
node_modules/

# Local-only
*.local

# Trial / commercial font binaries — never ship to public git.
# Webfont licences are required before bundling these in the repo.
*.woff
*.woff2
*.otf
*.ttf
*.eot
fonts/local-trials/
